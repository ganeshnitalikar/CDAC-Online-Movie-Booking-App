package com.moviebooking.entity;

/**
 * Enum representing different types of seats in a theatre.
 * Different seat types can have different pricing.
 */
public enum SeatType {
    NORMAL,
    PREMIUM
}
